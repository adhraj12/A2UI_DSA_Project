{
  "name": "neuroplay-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n ui,agent -c blue,green \"npm run dev:ui\" \"npm run dev:agent\"",
    "dev:ui": "npm run dev --workspace frontend",
    "dev:agent": "cd apps/agent && .\\venv\\Scripts\\python -m uvicorn src.server:app --port 8133 --reload",
    "install:agent": "cd apps/agent && python -m venv venv && .\\venv\\Scripts\\pip install -r requirements.txt",
    "postinstall": "npm run install:agent"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
